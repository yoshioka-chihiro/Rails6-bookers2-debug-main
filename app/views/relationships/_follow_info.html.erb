  <!--インフォのフォローボタン-->
    <% if current_user.following?(user) %>
      <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, remote: true, class: "btn btn-info btn-sm btn-block" %>
    <% else %>
      <%= link_to "フォローする", user_relationships_path(user.id), method: :post, remote: true,class: "btn btn-success btn-sm btn-block" %>
    <% end %>
